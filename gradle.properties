org.gradle.jvmargs=-Xmx1536m -Dfile.encoding=UTF-8
org.gradle.parallel=false

# Uncomment and point at a local JDK 21 if Gradle's toolchain auto-detection can't find one:
# org.gradle.java.home=/path/to/jdk-21
